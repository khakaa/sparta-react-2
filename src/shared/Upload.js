import React from "react";
import { Grid, Text, Button, Image, Input } from "../elements";

const Upload = (props) => {
  return (
    <>
      <input type="file" />
    </>
  );
};

export default Upload;
